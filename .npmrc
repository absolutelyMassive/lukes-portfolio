; Project-local npm config — committed so anyone (or any machine) running
; `npm install` here uses the public npm registry, regardless of any
; corporate / personal registry set in the user's global ~/.npmrc.
;
; This project has no private dependencies, so public npm is correct.
registry=https://registry.npmjs.org/
save-exact=true
