# No secrets required for this static portfolio site.
# Copy to .env.local only if you add optional integrations later.
#
# NEXT_PUBLIC_* variables are exposed to the browser — never put private keys here.
